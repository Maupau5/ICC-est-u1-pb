public class MetodoBusqueda {
    /*public int findEdad(Persona[] persona, int edad) {
        int inicio = 0;
        int fin = persona.length - 1;

        while (inicio <= fin) {

            int medio = inicio + (fin - inicio) / 2;

            if (persona[medio] == edad) {
                return medio;
            }
            if (persona[medio] < edad) {
                inicio = medio + 1; // buscar en la mitad de la derecha
            } else {
                fin = medio - 1; // buscar en la mitad de la izquierda
            }

        }

        return -1;
    }*/
}
